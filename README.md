# Darto Types

> This is used for Darto and for all package to be create a plugin for Darto.

- To install

```shell
dart pub add darto_types
```

- Add pubspec.yaml

```yaml
dependencies:
  darto_types: 0.0.3
```
